module Infer where

-- import Bergamot.Unification
-- import Bergamot.Parser (parseRule,parseQuery)


-- rs = map parseRule [
--   "EInt @ eval(?Rho,EInt(?N),VCons(?N)) <- int(?N);"
--   ] ++ map parseRule ["Int" ++ show n ++ " @ int(" ++ show n ++ ") <- ;" | n <- [0..10]]

-- q = parseQuery "eval(?Rho,EInt(1),?V)"